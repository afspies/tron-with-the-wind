services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        GIT_BRANCH: ${GIT_BRANCH:-}
    restart: unless-stopped
    expose:
      - "80"

  server:
    build: .
    restart: unless-stopped
    expose:
      - "8080"
    environment:
      - PORT=8080
