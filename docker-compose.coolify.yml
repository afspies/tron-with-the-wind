services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        GIT_BRANCH: ${GIT_BRANCH:-}
    restart: unless-stopped
    ports:
      - "80:80"

  server:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
